<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tws.repository.EmployeeMapper">
    <select id="selectAll" resultType="tws.entity.Employee">
        select * from `employee`;
    </select>

    <select id="queryEmployeeByKeyWorld" resultType="tws.entity.Employee">
        select * from employee
        where name like #{keyWorld} or age like #{keyWorld};
    </select>

    <insert id="insert">
        insert into employee(id, name, age)
        values(#{employee.id}, #{employee.name}, #{employee.age})
    </insert>

    <insert id="update">
        update employee set name = #{employee.name}
        where id = #{id}
    </insert>
</mapper>
